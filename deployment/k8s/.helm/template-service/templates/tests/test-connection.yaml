apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "template-service.fullname" . }}-test-connection"
  labels:
    {{- include "template-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "template-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
